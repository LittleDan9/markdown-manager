---
# Note: UI building and deployment is now handled by make deploy-ui
# This task only handles nginx configuration

- name: Create nginx web root directory
  shell: |
    sudo mkdir -p "{{ ui.nginx_root }}"
    sudo chown www-data:www-data "{{ ui.nginx_root }}"
    sudo chmod 755 "{{ ui.nginx_root }}"

- name: Ensure nginx directories exist
  shell: |
    sudo mkdir -p /etc/nginx/sites-available
    sudo mkdir -p /etc/nginx/sites-enabled
    sudo mkdir -p /etc/nginx/conf.d

- name: Deploy site-specific nginx configuration
  shell: |
    sudo cp "{{ playbook_dir }}/../nginx/sites-available/littledan.com.conf" /etc/nginx/sites-available/
    sudo chown root:root /etc/nginx/sites-available/littledan.com.conf
    sudo chmod 644 /etc/nginx/sites-available/littledan.com.conf

- name: Enable site configuration
  shell: |
    sudo ln -sf /etc/nginx/sites-available/littledan.com.conf /etc/nginx/sites-enabled/
    sudo rm -f /etc/nginx/sites-enabled/default

- name: Copy nginx configuration files
  shell: |
    sudo mkdir -p /etc/nginx/conf.d/
    sudo cp "{{ playbook_dir }}/../nginx/conf.d/"* /etc/nginx/conf.d/ 2>/dev/null || true
    sudo chown root:root /etc/nginx/conf.d/* 2>/dev/null || true
    sudo chmod 644 /etc/nginx/conf.d/* 2>/dev/null || true

- name: Copy nginx scripts
  shell: |
    sudo mkdir -p /etc/nginx/scripts/
    sudo cp -r "{{ playbook_dir }}/../nginx/scripts/"* /etc/nginx/scripts/ 2>/dev/null || true
    sudo chmod -R 755 /etc/nginx/scripts/ 2>/dev/null || true

- name: Test and reload nginx configuration
  shell: |
    sudo nginx -t && sudo systemctl reload nginx
  register: nginx_result
  failed_when: nginx_result.rc != 0

- name: Display nginx deployment status
  debug:
    msg: "✅ Nginx configuration deployed successfully"
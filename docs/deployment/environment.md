# Environment Configuration

The production environment template is at [deployment/production.env.template](../../deployment/production.env.template).

The actual secrets file (`deployment/production.env`) is gitignored and rsynced to the remote server during deployment. See [deployment/README.md](../../deployment/README.md) for details.
